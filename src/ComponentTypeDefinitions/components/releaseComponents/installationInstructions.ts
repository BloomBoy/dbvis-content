import makeComponent from '../../../utils/makeComponent';

interface InstallationInstructionsData {
}

const installationInstructions = makeComponent<InstallationInstructionsData>({
  name: 'Installation Instructions',
  subFields: {},
});

export default installationInstructions;
