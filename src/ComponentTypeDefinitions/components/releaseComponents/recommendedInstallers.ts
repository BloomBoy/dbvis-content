import makeComponent from '../../../utils/makeComponent';

interface RecommendedInstallerData {
}

const RecommendedInstallers = makeComponent<RecommendedInstallerData>({
  name: 'Recommended Installers',
  subFields: {},
});

export default RecommendedInstallers;
