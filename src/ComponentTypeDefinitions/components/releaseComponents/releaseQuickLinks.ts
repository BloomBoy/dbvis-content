import makeComponent from '../../../utils/makeComponent';

interface ReleaseQuickLinksData {
}

const ReleaseQuickLinks = makeComponent<ReleaseQuickLinksData>({
  name: 'Release quick links',
  subFields: {},
});

export default ReleaseQuickLinks;
