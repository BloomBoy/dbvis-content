import makeComponent from '../../../utils/makeComponent';

interface VersionSelectorData {
}

const VersionSelector = makeComponent<VersionSelectorData>({
  name: 'Version Selector',
  subFields: {},
});

export default VersionSelector;
